import type { Achievement } from '../types';

// Pending real content: client recommendation letter + Imagine Foundation
// letter. Populate with verifiable items only — never placeholder/invented
// entries. The Recognition section hides this subsection while empty.
export const achievements: Achievement[] = [];
